<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ali.khaleghi.daewooremote">

    <!-- ── بلوتوث ── -->
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN"
        android:usesPermissionFlags="neverForLocation" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADVERTISE" />

    <!-- ── شبکه WiFi — برای کنترل از طریق وای‌فای (پروتکل EShare) ── -->
    <!-- INTERNET: برای سوکت TCP به تلویزیون (پورت 2012) -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- ACCESS_WIFI_STATE: خواندن IP شبکه برای پیدا کردن تلویزیون -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <!-- ACCESS_NETWORK_STATE: بررسی اتصال شبکه -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- ── فرستنده مادون‌قرمز ── -->
    <uses-permission android:name="android.permission.TRANSMIT_IR" />
    <uses-feature android:name="android.hardware.consumerir" android:required="false" />

    <!-- ── لرزش (فیدبک لمسی) ── -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- سخت‌افزار اختیاری — اپ با یا بدون اینها کار می‌کند -->
    <uses-feature android:name="android.hardware.bluetooth_le" android:required="false" />
    <uses-feature android:name="android.hardware.bluetooth" android:required="false" />

    <application
        android:name=".DaewooApplication"
        android:label="کنترل هوشمند دوو"
        android:icon="@mipmap/ic_launcher"
        android:allowBackup="false"
        android:usesCleartextTraffic="true">
        <!--
          usesCleartextTraffic="true" برای اتصال TCP خام به تلویزیون در شبکه محلی
          لازم است. EShare هم از TCP ساده (بدون TLS) استفاده می‌کند چون شبکه
          داخلی خانه است و رمزنگاری لازم نیست.
        -->

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme" />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
</manifest>
